The input files must follow the style of the below example:
FD-1_c1_D1-1_n_afterFGA
FD-1_c1_D1-1 is the device ID
use "n" or "p" to designate positve or negative voltage sweep.
The rest is comment.

The code will merge both n and p sweep file into a single file and generate a summary